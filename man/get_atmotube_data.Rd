% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_atmotube_data.R
\name{get_atmotube_data}
\alias{get_atmotube_data}
\title{Retrieve Atmotube Data from the API (Single or Multi-Day)}
\usage{
get_atmotube_data(
  api_key,
  mac,
  start_date,
  end_date,
  order = "asc",
  format = "json",
  offset = 0,
  limit = 1440,
  separator = ",",
  base_url = "https://api.atmotube.com/api/v1/data"
)
}
\arguments{
\item{api_key}{A string containing the user's API key (required).}

\item{mac}{A string representing the MAC address of the Atmotube device (format: "aa:bb:cc:dd:ee:ff").}

\item{start_date}{A string representing the start date (format: "YYYY-MM-DD").}

\item{end_date}{A string representing the end date (format: "YYYY-MM-DD").}

\item{order}{A string indicating sorting order, either "asc" (ascending) or "desc" (descending). Default is "asc".}

\item{format}{A string specifying the response format, either "json" or "csv". Default is "json".}

\item{offset}{A non-negative integer specifying the starting index for pagination. Default is 0.}

\item{limit}{An integer (1-1440) defining the number of records per request. Default is 1440.}

\item{separator}{A string indicating the CSV separator when \code{format="csv"}. Default is ",".}

\item{base_url}{A string representing the API base URL (default: \code{"https://api.atmotube.com/api/v1/data"}).}
}
\value{
A list (if \code{format="json"}) or a concatenated CSV string (if \code{format="csv"}).
}
\description{
Fetches Atmotube air quality data from the Atmotube Cloud API.
Automatically switches to \strong{batch mode} if the requested date range exceeds 7 days,
ensuring compliance with API limits while preserving a \strong{one-day overlap}.
}
\examples{
\dontrun{
api_key <- "your_api_key_here"
mac <- "aa:bb:cc:dd:ee:ff"
start_date <- "2024-06-01"
end_date <- "2024-06-15"
data <- get_atmotube_data(api_key, mac, start_date, end_date)
print(data)
}
}
