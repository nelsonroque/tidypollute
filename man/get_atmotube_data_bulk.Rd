% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_atmotube_data.R
\name{get_atmotube_data_bulk}
\alias{get_atmotube_data_bulk}
\title{Retrieve Atmotube Data for Large Date Ranges with Overlap}
\usage{
get_atmotube_data_bulk(
  api_key,
  mac,
  start_date,
  end_date,
  order = "asc",
  format = "json",
  offset = 0,
  limit = 1440,
  separator = "semicolon"
)
}
\arguments{
\item{api_key}{A string containing the user's API key.}

\item{mac}{A string representing the MAC address of the Atmotube device.}

\item{start_date}{A string representing the start date (format: "YYYY-MM-DD").}

\item{end_date}{A string representing the end date (format: "YYYY-MM-DD").}

\item{order}{A string indicating sorting order, either "asc" or "desc". Default is "asc".}

\item{format}{A string specifying the response format, either "json" or "csv". Default is "json".}

\item{offset}{A non-negative integer specifying the starting index for pagination. Default is 0.}

\item{limit}{An integer (1-1440) defining the number of records per request. Default is 1440.}

\item{separator}{A string indicating the CSV separator when \code{format="csv"}. Default is "semicolon".}
}
\value{
A combined list (if \code{format="json"}) or concatenated CSV string (if \code{format="csv"}).
}
\description{
Fetches Atmotube air quality data over a date range exceeding the API's 7-day limit,
ensuring a one-day overlap between consecutive requests.
}
\examples{
\dontrun{
api_key <- "your_api_key_here"
mac <- "aa:bb:cc:dd:ee:ff"
start_date <- "2024-06-01"
end_date <- "2024-07-01"
data <- get_atmotube_data_bulk(api_key, mac, start_date, end_date)
print(data)
}
}
